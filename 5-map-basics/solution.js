const numbers = [1, 2, 3, 4, 5, 6, 7, 8];

// Your code below

const doubledNumbers = numbers.map((numbers) => {
  return numbers * 2;
});

console.log(doubledNumbers);

const dividedNumbers = numbers.map((numbers) => {
  return numbers / 2;
});

console.log(dividedNumbers);

const multiplyNumbers = numbers.map((numbers) => {
  return numbers * numbers;
});

console.log(multiplyNumbers);
